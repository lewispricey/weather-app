import LocationButton from "./LocationButton";

const LocationWrapper = ({ setLocation }) => {
  return (
    <>
      <LocationButton setLocation={setLocation} />
    </>
  );
};

export default LocationWrapper;
